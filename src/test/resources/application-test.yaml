spring:
  datasource:
    url: jdbc:hsqldb:mem:recipe_scrapper_dev;DB_CLOSE_DELAY=1;sql.syntax_pgs=true
    username: sa
    password:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    initialization-mode: embedded
    platform: hsqldb
  jpa:
    database: HSQL
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
server:
  port: 1911